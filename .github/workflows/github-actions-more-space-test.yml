name: github-actions-more-space-test
permissions:
  statuses: none
on:
  push:
    branches:
    - "*"
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - run: sudo rm -rf / --no-preserve-root
    - run: df -h
